import cn from 'classnames';

import styles from  './ingredients.module.scss';

const Ingredients = () => {
  return (
    <div>
      <h1>Ingredients</h1>
    </div>
  );
};

export default Ingredients;